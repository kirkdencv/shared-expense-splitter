# EXAMPLE ENV FILE - DO NOT USE THESE VALUES IN PRODUCTION
# Copy this file to .env and set real, secure values for production use.

# MongoDB Connection
MONGO_URI=mongodb+srv://<your-username>:<your-password>@<your-cluster>.mongodb.net/shared-expense-splitter?retryWrites=true&w=majority
# WARNING: Replace <your-username>, <your-password>, and <your-cluster> with your real values.

# JWT Secret (Use a strong, random string! Never commit your actual secret.)
JWT_SECRET=CHANGE_ME_TO_A_SECURE_RANDOM_STRING

# Server Port
PORT=3000

# --- SECURITY NOTICE ---
# Never commit your real secrets or credentials to version control.
# Always use strong, unique values for production environments.
